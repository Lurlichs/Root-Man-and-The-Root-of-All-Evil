using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Doesn't actually do anything, this is just used so that we can find the onion's
// eye using GetComponentInChildren<OnionEye>
public class OnionEye : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
